language: node_js
cache:
  directories:
    - ~/.npm
node_js:
  - '9'
before_install: yarn global add now
before_script: yarn validate
script: yarn build:prod
deploy:
  provider: script
  script: yarn deploy --token $NOW_TOKEN && yarn alias --token $NOW_TOKEN
  on:
    branch: master
